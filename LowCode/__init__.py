# lowcode/__init__.py
"""
LowCode Django App Package.

This package provides dynamic model generation, method binding,
and low-code platform capabilities.
"""

__version__ = "1.0.0"
__author__ = "Icent Team"

# Optional: Explicitly declare default app config (Django < 3.2 compatibility)
# In Django 3.2+, this is inferred from `default_app_config` in setup.py or via `AppConfig.name`
# But if you want to be explicit (or support older versions), uncomment below:

# default_app_config = 'lowcode.apps.LowCodeConfig'